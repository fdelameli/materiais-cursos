public interface Comparable {
    // retorna 0 se os dois elementos forem iguais.
    // retorna -1 se o elemento passado como parâmento for maior.
    // retorna 1 se o elemento passada como parâmento for menor.
    public int compareTo(Comparable obj);
}