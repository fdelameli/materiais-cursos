#include <16F877A.h>
#fuses xt, nolvp, protect
#use delay(clock=4Mhz)

void main() {
   
   while(true) { 
   
      if (input(pin_b1)) {
         output_high(pin_a0);
      } else {
         output_low(pin_a0);
      }  
   
   }

}
