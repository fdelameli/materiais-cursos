#include <16f877A.h>
#fuses xt, nolvp, protect
#use delay(clock=4Mhz)

void main() {

   while(true) {
   
      if(input(pin_a0) || input(pin_a2)) {
         output_high(pin_b0);
      } else {
         output_low(pin_b0);
      }
   
   }

}
